schema {
    query: Query
}

type Query {
    getAll: [Person]
    findOne(email: String): Person
}

type Person {
    id: String
    name: String
    mobile: String
    email: String
    address: String
}